/// Function hello accepts an argument and returns Hello argument followed by an exclamation.
/// It accepts string literal and returns an string object
pub fn hello(s: &str) -> String {
    format!("Hello {s} !")
}

